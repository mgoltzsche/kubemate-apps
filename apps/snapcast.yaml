apiVersion: apps.kubemate.mgoltzsche.github.com/v1alpha1
kind: App
metadata:
  name: snapcast
spec:
  kustomization:
    sourceRef:
      kind: GitRepository
      name: kubemate-apps
    path: ./config/snapcast
